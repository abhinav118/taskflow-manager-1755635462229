interface AddTaskButtonProps {
  variant?: any
  size?: any
  icon?: any
}

export default function AddTaskButton({ variant, size, icon }: AddTaskButtonProps) {
  return (
    <div className="">
      {/* AddTaskButton component content */}
    </div>
  )
}