interface TaskModalProps {
  size?: any
  closable?: any
}

export default function TaskModal({ size, closable }: TaskModalProps) {
  return (
    <div className="">
      {/* TaskModal component content */}
    </div>
  )
}